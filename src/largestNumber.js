function solution(n) {
    return parseInt('9'.repeat(n));
}

module.exports = solution;

// npm init -y
// npm i jest-cli -g
// package.json: "scripts": { "test": "jest" }
// npm test largestNumber

// alternative solution
